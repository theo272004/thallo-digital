# Tool prototype — contains secrets (.env with API keys), node_modules,
# and the leads database. NEVER publish this to GitHub Pages.
brand-seo-snapshot-prototype/

# Local Claude Code dev config (launch.json, etc.)
.claude/

# Node / React app build artifacts
node_modules/
app/dist/

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
