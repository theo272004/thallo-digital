# Copy to .env.local for development, and set the same values in the deploy
# workflow for a build that goes live. This is a static export, so both are read
# at BUILD time and baked into the HTML — changing them means rebuilding.

# Where the WordPress plugin serves the scan API.
# Leave this unset and the tool runs on sample data with a "preview mode" banner.
#
#   Bluehost, WordPress at /blog/:  https://thallodigital.com/blog/wp-json/thallo/v1
#   WordPress on its own subdomain: https://cms.thallodigital.com/wp-json/thallo/v1
#   Local WordPress:                http://localhost:8080/wp-json/thallo/v1
#
# Anything other than same-origin also needs the site's origin added to
# "Allowed origins" on the plugin's settings screen.
NEXT_PUBLIC_SCAN_API=

# GA4. The Measurement ID from Admin -> Data streams, shaped G-XXXXXXXXXX.
#
# Read at BUILD time like everything else here, so changing the property means
# a rebuild. Left unset, no tag is emitted at all — which is what `npm run dev`
# and the GitHub Pages mirror both want: neither should be sending traffic to
# the property that reports on the real site.
#
# One property covers both systems on the domain. The WordPress blog at /blog/
# carries the same ID, so a visitor who reads an article and then opens
# /thallo-ai/scan/ is one session, not two.
NEXT_PUBLIC_GA_ID=

# WordPress, for editing the blog from this repo with scripts/blog.mjs.
#
# WP_APP_PASSWORD is an Application Password, not the WordPress login:
#   /blog/wp-admin/profile.php → Application Passwords → Add New
# WordPress generates it, it only works against the REST API, and it can be
# revoked from that same screen without changing anything else about the
# account. Keep the spaces exactly as WordPress shows them.
#
# Only needed to write posts. Nothing in the site build reads these.
WP_USER=
WP_APP_PASSWORD=

# Not normally set by hand. `next.config.ts` derives it from DEPLOY_TARGET and
# exports it, so that basePath and every hand-written href agree:
#
#   DEPLOY_TARGET=bluehost  →  ''                (site at the domain root)
#   anything else           →  '/thallo-digital' (GitHub Pages project path)
#
# NEXT_PUBLIC_BASE_PATH=
